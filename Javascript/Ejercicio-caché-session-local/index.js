const nombre = "Oscar";
const apellido = "Flores";

const info_personal = {nombre: "Oscar",
apellido: "Flores"}

// sessionStorage.setItem("perfil", JSON.stringify(info_personal))
// localStorage.setItem("perfil1", JSON.stringify(info_personal))

const fecha = new Date()
// document.cookie = `perfil3=${JSON.stringify(info_personal)};expires=${new Date(fecha.getTime() + 2 * 60000)}`

